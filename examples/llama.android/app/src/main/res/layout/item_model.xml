<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="8dp"
    app:cardCornerRadius="12dp"
    app:cardElevation="2dp"
    android:contentDescription="模型卡片">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <!-- Model Name and Size -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:contentDescription="模型信息">

            <LinearLayout
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:orientation="vertical">

                <TextView
                    android:id="@+id/modelNameText"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:textSize="16sp"
                    android:textStyle="bold"
                    android:textColor="?attr/colorOnSurface"
                    tools:text="gemma-2-2b-it-q4_k_m"
                    android:contentDescription="模型名称"
                    android:importantForAccessibility="yes" />

                <TextView
                    android:id="@+id/modelSizeText"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="4dp"
                    android:textSize="14sp"
                    android:textColor="?attr/colorOnSurfaceVariant"
                    tools:text="1.5 GB"
                    android:contentDescription="模型文件大小"
                    android:importantForAccessibility="yes" />

            </LinearLayout>

            <!-- Status Indicator -->
            <TextView
                android:id="@+id/statusText"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:paddingHorizontal="12dp"
                android:paddingVertical="4dp"
                android:textSize="12sp"
                android:textStyle="bold"
                android:background="@drawable/status_background"
                android:textColor="@android:color/white"
                tools:text="已加载"
                android:visibility="gone"
                tools:visibility="visible"
                android:contentDescription="模型状态"
                android:importantForAccessibility="yes" />

        </LinearLayout>

        <!-- Action Buttons -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:orientation="horizontal"
            android:contentDescription="模型操作按钮">

            <!-- Load/Unload Button -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/loadUnloadButton"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:layout_marginEnd="8dp"
                android:text="加载"
                android:textSize="14sp"
                style="@style/Widget.Material3.Button"
                android:contentDescription="加载此模型" />

            <!-- Delete Button -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/deleteButton"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:layout_marginStart="8dp"
                android:text="删除"
                android:textSize="14sp"
                style="@style/Widget.Material3.Button.TonalButton"
                app:backgroundTint="?attr/colorErrorContainer"
                app:strokeColor="?attr/colorError"
                android:contentDescription="删除此模型" />

        </LinearLayout>

    </LinearLayout>

</com.google.android.material.card.MaterialCardView>
